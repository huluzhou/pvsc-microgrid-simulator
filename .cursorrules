# 项目 AI 规则

## Commit 信息（必须遵守）
生成 commit message 时：**必须使用简体中文**，禁止英文。格式：`类型(范围): 描述`，描述需具体完整。
- Cursor 内置「Generate commit message」不读取规则，请用 Chat 输入 `/生成Commit` 调用自定义命令。

## 代码与界面
- 注释用中文；变量/函数名用英文。
- 用户界面文案使用中文。
